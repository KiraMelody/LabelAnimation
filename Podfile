# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'LabelAnimation' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LabelAnimation
  pod 'UICountingLabel'
  target 'LabelAnimationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LabelAnimationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
